# Fable precision review and evidence disposition

Actual Claude Code claude-fable-5-1, max, existing Claude.ai subscription completed a bounded one-turn code/evidence review in305.76seconds, exit0, is_error=false. No tools/subagents or private audio/transcripts were supplied in this successful call. The earlier broad Fable and Opus tool-enabled runs timed out without reviews. Local full response: build/benchmarks/fable-bounded-precision-review.md. This is a metric/evidence review, not native model validation.

Accepted: inspect paired case deltas, normalization sensitivity, entity coverage and reference provenance before promoting a vocabulary/model change. Offline script scripts/audit-fleurs-paired.py selects the same first successful stored public outputs, verifies the original244words and14/17errors, and records source hashes. No inference or rescoring defaults changed.

Results: all three extra vocabulary-off edits are concentrated in two of twelve cases. A seeded20,000-draw paired utterance bootstrap yields an off-minus-on WER interval of0 to0.0287546 (0–2.88percentage points). This is descriptive for the first12 inspected read-speech cases, not a population/generalization interval. Removing straight/curly apostrophes as a separate diagnostic changes on/off to11/234 and12/234. The original14/244 and17/244 remain authoritative canonical scores. Combining-dot U+0307 count is zero across these references and both hypotheses; the proposed Unicode edge case is not demonstrated here.

Corrections to the review: apostrophe/number handling is the stated canonical policy, not automatically a scoring defect. Nested closed-universe types and legacy entity recall have documented semantics; do not silently change them. The existing vocabulary predates this inspected FLEURS sample; contamination was not established. No temperature fallback/nondeterminism claim is accepted without checking actual decoder options/output. No inference that a word containing q/w/x is code-switching. Do not adopt the proposed capitalization-only universe as human name gold. The review's unconditional NIST-equivalence claim for DER is not independently verified by this review or this audit.

Next accuracy gate: preserve canonical scoring; separately annotate held-out human meeting/name/code-switch audio before any model promotion, freeze vocabulary/universe before decoding, report unique-type precision/recall and audited false additions, and retain failures/deferred jobs alongside paired scores. Any larger public FLEURS sample would remain read-speech evidence and requires resource admission; it cannot replace this meeting gate. Full recording recovery and live latency remain unresolved.
