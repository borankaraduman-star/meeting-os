# Isolate final speaker identity embeddings — implemented, long-record validation pending

Observed: current real retry terminal at69/392, OS pressure while native CPP uses~1.2GB and parent retains Python/embedding state. Fresh process measurement: baseline imports/CPP adapter75,530,792bytes; after eager Resemblyzer init245,695,352bytes (+170,164,560bytes/~162MiB). This measures initialization residency, not total later peak or proof of the sole pressure cause. Evidence benchmarks/results/embedding-parent-residency-2026-09-09.json.

Narrow scope: retry, CPP, isolated Sherpa, Resemblyzer only. Use lightweight model-id/weight resolver without importing Torch/Resemblyzer runtime in parent. Keep exact hash/model-ID format. Finish each source ASR first; preserve existing segment text/times/flags/order and exact embedding sample bounds and ambiguity exclusion. Then one bounded direct worker loads Resemblyzer and computes that source's vectors in the original order from the same private immutable float32 snapshot. Worker exits before next source's ASR. Parent applies store.identify in original order with identical thresholds/model ID. Default live, cluster/ECAPA and normal imports remain unchanged. No auto enrollment or paid API.

Worker contract: bounded frame/span/request counts and output bytes, explicit indexes and exact count, finite valid vectors or None only, no partial success, snapshot/weight signatures plus model-ID verified before/after. Use existing supervisor/lifeline, OS-pressure/footprint guards and cancellation. No extra complete audio array; seek/read only exact segment samples. Pipeline result list is already held per source; retain only bounded per-segment sample-span descriptors, not audio copies. Full retry commit remains atomic; failed worker leaves original transcript and completed ASR/diarization checkpoints intact.

Acceptance: failing tests for no parent embedding load before/during ASR, all ASR calls before first embedding, exact spans/clipping/ambiguity/short audio, matching segments and saved identity decisions, multiple sources do not retain worker, child failure/cancellation cannot commit partial output, profile model hash equality, changed weight/snapshot rejects. Native public audio baseline vs worker vectors with numerical difference recorded and identical identity decisions; real process residency beforeASR lower using physical footprint (notRSS). Then complete real retry and evaluate live separately.

Claude medium-effort review emphasized partial-worker failure, index/count verification and hash parity. Rejected suggestions: only mic/system sources exist (not one source per detected speaker); reusing a worker across sources retains model memory during next ASR, defeating purpose. Snapshot already immutable before ASR and is never changed by retries; taking a fresh snapshot after ASR is unnecessary. IPC ordered list does not spontaneously reorder, but explicit count/index validation remains useful. Use physical footprint, not Claude's suggestedRSS-only measure.

## Implementation and corrected memory attribution

The initial plan missed that Silero VAD itself imports Torch. Isolating only Resemblyzer would not establish a Torch-free ASR parent. The supported retry path now uses both FinalEmbedder and isolated final VAD. Unchanged Silero runs on the same immutable FLOAT snapshot in a supervised direct child and returns validated sample spans. Parent avoids the full audio mmap on this path; then isolated Sherpa, all source ASR, and an isolated identity batch run sequentially. Each worker exits before the next phase/source. Default live/import/cluster/ECAPA/larger-Mac behavior stays unchanged. Existing69ASR checkpoints and schema2diarization keys remain valid; no model/decoder change.

Identity span cap60s (currentVADmax28s), request cap10000spans, output cap32MiB; exact model hash, snapshot/weights signatures, indexes/counts/finite256Dunit vectors, no partial commit. Short spans returnNone; ambiguous segments never request an embedding. Cancellation uses existing direct-child supervision/lifeline. Temporary data stays local.

30 relevant tests passed. Native two vectors match exactly and a fixture profile produces same decisions. Full10s public VAD/Sherpa/CPP/identity comparison:3segments,2turns,2nonnull vectors exactly match, including confidence flags/timestamps. First ASR parent footprint268,403,624→92,897,952bytes; final observations302,138,400→93,127,352. Torch absent from isolated parent at all ASR observations. This is parent residency, not a bound on total OS/child memory. Cold short test elapsed12.43→14.34s: memory improvement, not a speed claim. Separate95.58s public native VAD comparison returns identical13sample spans. Evidence final-identity-native, final-phases-native and final-vad-native JSONs under benchmarks/results, dated2026-09-09.

Claude Sonnet architecture follow-up asked about sample bounds/units: validate_regions enforces integer0<=start<end<=frames; identities use exact sample spans and fixtures check nonzero offset/clipping. The first code-review attempt returned unusable tool-call text, so it is not counted as a completed review. Stronger Opus/max independent source/agent review is running in a sanitized copy (session80775). User subsequently requested Fable; Fable5.1/max is running a distinct Turkish precision/measurement audit (session95385). The recurring quality heartbeat now prefers Fable5.1/max for consequential review. Claude.ai Max authentication verified; no API credentials used. Do not claim subagent completion until its actual output confirms it.
