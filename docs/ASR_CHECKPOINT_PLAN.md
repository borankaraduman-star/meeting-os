# Retry-only ASR checkpoints — implementation next

Evidence: long real retry now completed full diarization, but OS pressure interrupted at transcribing index6of392 after253.23s, sampled tree peak2,125,843,816bytes. Existing270provisional segments remain. Re-running every completed native call discards useful computation. This is a resumption change, not a claim of faster native inference or higher accuracy.

Scope: CPP raw-ASR results only in same-meeting retry; no cache in live, ordinary import, benchmarks, speaker embeddings or identity matching. Recompute diarization/identity each attempt so profile changes apply. Existing source/copy digest verification and atomic final replacement remain authoritative.

Persist completed validated raw row JSON atomically in SQLite, keyed by meeting plus exact contiguous float32 input bytes/rate and effective ASR identity (model/binary content identity, decoder flags including threads/flash/GPU, prompt/language and adapter/parser revision). Detect model/binary changes before lookup/after compute and reject changed-identity results, not just their cache write. Never store failed/partial native results. Cache errors must not silently fabricate output. Bound each payload and total retained payload; deterministic LRU eviction, distinguish misses/corrupt entries from valid empty output. Cache hit returns a newly decoded value so downstream changes cannot mutate stored rows.

Use FK meeting cascade with foreign_keys enabled, unique compound key and atomic UPSERT. Global64MiB payload budget; keep counting serialized bytes separately from physical SQLite/WAL size. Cache hits/misses must be visible in retry diagnostics; do not use warm-cache timings as model performance evidence. Existing RetryStore snapshot covers meeting metadata/segments, not cache rows, so cache writes must not alter the protected transcript baseline. No native work while a retry/recording is live.

Required tests: completed-first-region plus second-region failure then retry skips only exact first-region compute; changed audio/options/model invalidate; malformed/oversized cache rejected; empty result valid; model change mid-call rejects result; real FK deletion; bounded eviction; cached and uncached rows preserve ordering/timestamps and recomputed identity; failure leaves original transcript and successful checkpoints intact. Native bounded sanity check before another long retry. Do not relax OS pressure or memory limits and do not auto-loop retry under pressure.

Claude Code reviewed the bounded design. It reinforced decoder/environment identity completeness, exact-slice hashing, explicit eviction and uniqueness, failure policy for changing model identity, real cascade tests and ordered mixed hit/miss assembly. Implementation not started in this checkpoint.
