# UI freeze investigation — 22:22–22:26 TRT

User reported a frozen app. PID 44892 was at 98.4% CPU, ~750 MiB footprint. Capture/worker processes had already exited; current meeting 553993e7544a is incomplete. Sample `/tmp/meetingos-freeze.sample` captured 65/65 main-thread samples in SwiftUI layout/AttributeGraph, with repeated SizeFittingLayoutComputer measurement. This establishes main-thread layout saturation, not an inference worker deadlock. Exact long-duration trigger has not been reproduced in isolation.

Contributors found in code: every transcript row observed the entire model, every snapshot replaced the row array, and every row used alternative ViewThatFits layouts. On process failure, the entire job log was read on the main actor and its final line (25,618 characters of repeated errors in this incident) passed to the error banner. Claude Code reviewed numeric evidence and architecture only; its speculative graph-leak diagnosis is NOT established. The memory-pressure errors came from actual worker admission checks, not AttributeGraph diagnostics.

Changes installed with the existing Apple Development identity:
- Fixed two-line speaker/source header, no ViewThatFits in transcript rows.
- Equatable row rendering with explicit playback/edit state; unchanged snapshot rows are not republished.
- Read at most 8 KiB of job-log tail off the main actor. Bound displayed errors to 400 characters; full log stays on disk.
- Existing committed preview-failure/final-handoff fixes now present in fresh processes. Live GPU experiments disabled; tuning profile absent.

Verification: 40 Swift tests pass, including a 300-row / 25k-character failure / resize layout regression (~0.15s). 20 Python live tests pass. Signed release installed and reopened. Actual latest meeting displays 270 provisional segments; native accessibility confirms record, transcript and recover controls fit the minimum-size window. Screenshot inspected at `/tmp/meetingos-freeze-fixed.png`. App CPU dropped to low single digits after reopening; this is recovery evidence, not proof of a long-run fix. No capture process was terminated by this repair; only the already-frozen UI was restarted after checking ownership.

526 WAV files exist, all larger than their headers (~1.82 GB). This is presence/size verification, not decoded integrity or transcript accuracy. Final transcription remains pending. Do not call the recording complete. Long-meeting soak, failure injection in a displayed window and sustained live latency remain validation gaps. The previous live GPU trial hit memory protection and may have contributed preview errors; do not repeat under live user load.
