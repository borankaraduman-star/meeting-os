# Release diarization memory before ASR

Stage profiling found parent memory retained after native Sherpa processing, unaffected by Python GC. A direct guarded worker now handles Sherpa on Mac hardware <=16GiB through `make_pipeline`; other diarizers and larger machines retain prior behavior. The public Diarizer option defaults false so worker execution cannot recurse. Same model paths/config, threshold, float32 audio and turn labels; no ASR/identity-model change. Parent loads only Sherpa config, not the native processing instance.

Input is an exact FLOAT mono16k temporary snapshot. Required disk plus100MiB checked before writing. Direct child shares the outer CLI process group so its existing lifeline owns it; run_guarded enforces600s, pressure, and memory limits. Worker starts no further children. TemporaryDirectory removes inputs/results on success, validation error and normal guarded failure. Abrupt kill can leave this private temporary directory, as with existing ASR temporary files; no blanket orphan-cleanup claim. Turn output is bounded to4MiB/10k entries, finite start-ordered within duration, correct source/S-number labels. Overlapping turns are deliberately preserved.

Native comparison on95.58s public constructed four-voice audio, production threshold.9: identical13turns/fourdetected speakers. Parent after diarization:674,284,864→268,190,632 bytes (~387MiB released). Sampled tree peak during diarization rises685,573,920→800,655,784 bytes because parent and worker coexist. Whole probe5.54→6.30s, single-order timing. Benefit targets resident memory during subsequent ASR, not faster diarization. Six wrapper/routing tests include unchanged audio, output validation, cleanup, low-memory factory policy and overlap retention; prior combined43tests passed before the overlap test was added.

Actual first finalized real-mic chunk completed both serial and window-enabled trials under isolation (sampled tree peak1,517,521,896 bytes). Three ASR calls in both; no grouping eligible, exact text digest equality and identical turns.16.26 vs15.74s is not window acceleration. No human gold or stored-profile acceptance in this probe. Meeting DB/original recording untouched.

Claude reviewed worker code with low effort and returned concerns. Corrected interpretation: next start is compared to prior start intentionally because diarization overlap is valid; added regression to preserve it. JSONDecodeError is a ValueError subclass. Custom model paths are already authorized/config-validated, so no arbitrary directory allowlist added. Disk preflight does not reserve against concurrent writers; write failure propagates and retry keeps original transcript. Review is evidence, not unqualified approval.

Long real-meeting recovery still requires successful completion. Stage reports and real-chunk numeric results are under benchmarks/results/. No private audio/transcripts sent to Claude.
