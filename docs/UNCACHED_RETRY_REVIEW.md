# Fable uncached retry experiment review

Actual Claude Code Fable5.1/max completed source-only review18374 with exit0; no private audio or transcript supplied. Full local response: build/benchmarks/fable-uncached-review.md. This is not approval of the full recording result: the experimental full retry still stopped at VAD under memory pressure.

Applied: the experiment now logs its first successful F_NOCACHE application separately for source and destination descriptors, with counters returned to validation callers. Native1MiB transfer verifies both counters=1, exact copy/hash and unchanged source. Interruption callback now filters the requested meeting id in addition to pid. Regression with two meetings sharing a historical pid proves only the target is marked, DB closes, and worker exception propagates. Capture advisory guard also recognizes a bare MeetingCapture command. Use exported fcntl.F_NOCACHE if present, SDK fallback48 otherwise. Resource output key explicitly says experimental_retry_resources.

Rejected/corrected review claims: run_guarded raises on nonzero workers and uncaught exceptions produce nonzero exit; actual failed experiment exited1. The narrow wrapper matches the actual frozen _transfer signature; hypothetical future API changes should fail closed rather than silently broaden compatibility. The measured order was uncached THEN normal, not the reverse stated by Claude. Comparison remains single-order and is not causal proof. Process-list guard is advisory; actual same-meeting exclusion is provided by RetryStore BEGIN IMMEDIATE, retry_one_running unique index, owner identity and protected transcript checks. Supervisor kills the owned group and reaps the direct child before on_failure; no stronger claim of every descendant being reaped was established here.

Production defaults and memory limits remain unchanged. Full recording recovery, sustained live latency and real held-out meeting accuracy remain unverified. No more heavy retry was launched for this review.
